<div class="well">
    <ul class="nav nav-list">
        <li
                {% if request.attributes.get('_route') == 'sorties_professeur' %}
                    class="active"
                {% endif %}
        >
            <a href="{{ path('sorties_parent') }}" class="sorties">Sorties à venir</a>
        </li>

        <li
                {% if request.attributes.get('_route') == 'anciennes_sorties_parent' %}
                    class="active"
                {% endif %}
        >
            <a href="{{ path('anciennes_sorties_parent') }}">Sorties passées</a>
        </li>
    </ul>
</div>

