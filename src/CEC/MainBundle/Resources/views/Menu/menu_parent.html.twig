<div class="navbar navbar-default navbar-fixed-top ">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{path('cec_externe_accueil')}}" style="padding-top:0; padding-bottom:0;">
                <img alt="Logo CEC" src="{{asset('bundles/cecmain/images/logorond.png')}}" style="max-height:50px;"/>

            </a>

        </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

            <ul class="navbar-nav nav">
                <li><a href="{{ path('tableau_de_bord_parent') }}">Tableau de bord</a></li>
                <li><a href="{{path('toutes_seances') }}">Planning de CEC</a></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                        Le tutorat
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        {% for lycee in lycees %}
                        <li>
                            <a href="{{ path('lycee', { 'lycee' : lycee.id} ) }}">
                                {{lycee.nom}}
                            </a>
                        </li>
                        {% endfor %}
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                        Projets
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('description_projets')}}">Pôle Projets</a></li>
                        <li class="secteur">Les Projets</li>
                        {% for projet in projets %}
                            <li><a href="{{ path('description_projet', {'slug' : projet.slug } ) }}"> {{projet.nom}}</a></li>
                        {% endfor %}
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                        Sorties & Evènements
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{path('toutes_seances')}}"> Planning </a> </li>
                        <li class="secteur"> Les sorties </li>
                        <li><a href="{{path('sorties_professeur')}}"> Liste des sorties</a></li>
                    </ul>
                </li>
            </ul>

            <div class="btn-group pull-right">
                <a class="btn btn-default navbar-btn" href="{{ path('voir_parent', { 'id' : parent.id }) }}">
                    {{ parent }}
                </a>
                <a class="btn btn-default navbar-btn dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li><a href="{{ path('reglages_infos_parent') }}">Modifier mon profil</a></li>
                    <li class="divider"></li>
                    <li><a href="{{ path('deconnexion') }}">Se déconnecter</a></li>
                </ul>
            </div>
        </div>
    </div>
</div>
