<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a href="{{ path('cec_externe_accueil') }}" class="brand">CEC</a>

			<ul class="nav">
                <li><a href="{{ path('tableau_de_bord_professeur') }}">Tableau de bord</a></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        Le tutorat
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a href="{{ path('lycee', { 'lycee' : professeur.lycee.id} ) }}">
                                {{professeur.lycee.nom}}
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('toutes_seances') }}">
                                Planning des séances
                            </a>
                        </li>
                    </ul>
                </li>

				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">
								Projets
								<b class="caret"></b>
					 </a>
					<ul class="dropdown-menu">
                        
                        <li><a href="{{ path('toutes_seances') }}">Planning</a></li>
                        <li><a href="{{ path('description_projets')}}">Pôle Projets</a></li>
                        <li class="secteur">Les Projets</li>
                        {% for projet in projets %}
                            <li><a href="{{ path('description_projet', {'slug' : projet.slug } ) }}"> {{projet.nom}}</a></li>
                        {% endfor %}
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                Sorties & Evènements
                                <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{path('toutes_seances')}}"> Planning </a> </li>
                        <li class="secteur"> Les sorties </li>
                        <li><a href="{{path('sorties_professeur')}}"> Liste des sorties</a></li>
                    </ul>
                </li>
            </ul>
        
    		<div class="btn-group pull-right">
                    <a class="btn btn-inverse" href="{{ path('voir_professeur', { 'id' : professeur.id }) }}">
                        {{ professeur }}
                    </a>
                    <a class="btn btn-inverse dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('reglages_infos_professeur') }}">Modifier mon profil</a></li>
                        <li class="divider"></li>
                        <li><a href="{{ path('deconnexion') }}">Se déconnecter</a></li>
                    </ul>
    		</div>
        </div>
	</div>
</div>
