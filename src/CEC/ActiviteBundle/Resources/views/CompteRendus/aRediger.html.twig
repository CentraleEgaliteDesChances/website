{% extends 'CECMainBundle::narrow.html.twig' %}


{% block narrow_title %}Compte-rendus à rédiger{% endblock %}

{% block narrow_body %}

{{ parent() }}

<table class="table table-bordered table-hover table-striped">
    {% if compte_rendus|length > 0 %}
        <thead><tr><th style="font-weight: normal;">
            <p><strong>Rédiger les compte-rendus de séance, c'est important !</strong></p>
            <p>Grâce aux compte-rendus que les tuteurs remplissent à chaque séance, les secteurs de l'association peuvent améliorer sans cesse les activités que vous proposez aux lycéens. Mieux : cela aide aussi les autres tuteurs à choisir l'activité la plus adaptée pour leur groupe.</p>
            <p>Remplir un compte-rendu prend 10 secondes, alors n'hésitez pas !</p>
        </th></tr></thead>
        <tbody>
            {% for cr in compte_rendus %}
            <tr>
                <td>
                    <span class="badge badge-important pull-right">1</span>
                    <a href="{{ path('seance', { 'seance': cr.seance.id }) }}">
                        <strong>Compte-rendu de la séance du {{ cycle(jours, cr.seance.date|date('N'))|lower }} {{ cr.seance.date|date('j') }} {{ cycle(mois, cr.seance.date|date('n')) }}</strong><br />
                        Groupe de tutorat : {{ cr.seance.groupe }}
                    </a>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    {% else %}
        <thead><tr><th class="muted"><h4>Aucun compte-rendu n'est à rédiger !</h4></th></tr></thead>
    {% endif %}
</table>

{% endblock %}
