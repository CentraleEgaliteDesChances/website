{% extends 'CECMainBundle::narrow.html.twig' %}


{% block narrow_title %}Nouveau membre{% endblock %}

{% block narrow_body %}

{{ parent() }}

<form class="form" method="post" action="{{ path('creer_membre') }}">
    {{ form_widget(form) }}
    <div class="footer-controls">
        <input type="submit" value="Créer le membre" class="btn btn-primary pull-right" />
        <a href="{{ path('creer_membre') }}" class="btn pull-right">Annuler</a>
    </div>
</form>

{% endblock %}
