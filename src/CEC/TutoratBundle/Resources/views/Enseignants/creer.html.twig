{% extends 'CECTutoratBundle:Enseignants:base.html.twig' %}


{% block narrow_title %}Nouvel enseignant{% endblock %}

{% block narrow_body %}

{{ parent() }}

<form class="form" method="post" action="{{ path('creer_enseignant') }}">
    {{ form_widget(form) }}
    <div class="footer-controls">
        <input type="submit" value="Créer l'enseignant" class="btn btn-primary pull-right" />
        <a href="{{ path('tous_enseignants') }}" class="btn pull-right">Annuler</a>
    </div>
</form>

{% endblock %}
