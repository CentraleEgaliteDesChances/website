{% extends 'CECMainBundle::narrow.html.twig' %}


{% block narrow_title %}Nouveau lycée{% endblock %}

{% block narrow_body %}

{{ parent() }}

<form class="form" method="post" action="{{ path('creer_lycee') }}">
    {{ form_widget(form) }}
    <div class="footer-controls">
        <input type="submit" value="Créer le lycée" class="btn btn-primary pull-right" />
        <a href="{{ path('tous_lycees') }}" class="btn pull-right">Annuler</a>
    </div>
</form>

{% endblock %}
