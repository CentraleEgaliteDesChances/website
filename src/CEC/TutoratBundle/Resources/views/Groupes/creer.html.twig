{% extends 'CECMainBundle::narrow.html.twig' %}


{% block narrow_title %}Nouveau groupe de tutorat{% endblock %}

{% block narrow_body %}

{{ parent() }}

<form class="form" method="post" action="{{ path('creer_groupe') }}">
    {{ form_widget(form) }}
    <div class="footer-controls">
        <input type="submit" value="Créer le groupe de tutorat" class="btn btn-primary pull-right" />
        <a href="{{ path('tous_groupes') }}" class="btn pull-right">Annuler</a>
    </div>
</form>

{% endblock %}
